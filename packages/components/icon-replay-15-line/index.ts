import type { App } from 'vue';
import _IconReplay15Line from './icon-replay-15-line.vue';

const IconReplay15Line = Object.assign(_IconReplay15Line, {
  install: (app: App) => {
    app.component(_IconReplay15Line.name, _IconReplay15Line);
  }
});

export default IconReplay15Line;