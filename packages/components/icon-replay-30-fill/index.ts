import type { App } from 'vue';
import _IconReplay30Fill from './icon-replay-30-fill.vue';

const IconReplay30Fill = Object.assign(_IconReplay30Fill, {
  install: (app: App) => {
    app.component(_IconReplay30Fill.name, _IconReplay30Fill);
  }
});

export default IconReplay30Fill;