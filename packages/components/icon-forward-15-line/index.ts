import type { App } from 'vue';
import _IconForward15Line from './icon-forward-15-line.vue';

const IconForward15Line = Object.assign(_IconForward15Line, {
  install: (app: App) => {
    app.component(_IconForward15Line.name, _IconForward15Line);
  }
});

export default IconForward15Line;