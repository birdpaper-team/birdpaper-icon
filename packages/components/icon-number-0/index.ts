import type { App } from 'vue';
import _IconNumber0 from './icon-number-0.vue';

const IconNumber0 = Object.assign(_IconNumber0, {
  install: (app: App) => {
    app.component(_IconNumber0.name, _IconNumber0);
  }
});

export default IconNumber0;