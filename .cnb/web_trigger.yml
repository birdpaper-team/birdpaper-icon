branch:
  - reg: "next|main"
    buttons:
      - name: npm publish
        description: 发布到 npm 仓库
        event: web_trigger_one
        inputs:
          version:
            name: 版本号
            description: 输入版本号
            required: true
            type: input
            default: 1.0.0
    buttons:
      - name: Docs publish
        description: 发布文档
        event: web_trigger_two